## 2026-03-11 - Task: Add thinking mode switch to ChatCore.vue

### What was done:
- Added `ElSwitch` component import to ChatCore.vue
- Added `thinkingMode` reactive state variable
- Added a Switch component with label "思考模式" in the input actions area
- Added CSS styles for the switch to ensure proper alignment
- Updated the `handleSend` function to include `thinkingMode.value` when sending messages via WebSocket

### Testing:
- The build was successful with no errors related to the changes
- The switch is properly positioned in the input actions area
- The switch state is bound to the `thinkingMode` variable
- The thinking mode state is now included in WebSocket messages

### Notes:
- The switch is placed between the upload button and the send button
- The switch is styled with a small label for clarity
- The thinking mode state is now passed to the backend via WebSocket messages
