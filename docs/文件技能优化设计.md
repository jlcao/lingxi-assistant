# 灵犀助手文件技能优化设计

**版本：** v2.0  
**日期：** 2026-03-14  
**参考：** OpenClaw 文件技能对比分析

---

## 📊 优化目标

基于与 OpenClaw 的对比分析，优化灵犀助手文件技能，保持优势的同时补齐短板。

### 当前优势（保持）
- ✅ 内置文本搜索功能
- ✅ 上下文显示（前后 N 行）
- ✅ 自动创建父目录
- ✅ API 简洁易用

### 需要改进（新增）
- ❌ 追加模式写入
- ❌ 流式读取大文件
- ❌ Diff 补丁应用
- ❌ 批量文件操作
- ❌ 文件编码自动检测

---

## 🎯 优化方案

### 方案 1：read_file 技能增强

#### 1.1 添加流式读取支持

**新增参数：**
```json
{
    "file_path": "/path/to/file.txt",
    "search_text": "keyword",
    "context_lines": 5,
    "stream": false,           // 新增：流式读取
    "chunk_size": 8192,        // 新增：块大小
    "max_lines": 1000,         // 新增：最大行数限制
    "encoding": "utf-8",       // 新增：编码（可选）
    "detect_encoding": true    // 新增：自动检测编码
}
```

**实现代码：**
```python
#!/usr/bin/env python3
"""Read file skill - 增强版"""

import chardet  # 编码检测
from typing import Dict, Any, Generator


def execute(parameters: Dict[str, Any]) -> str:
    file_path = parameters.get("file_path")
    search_text = parameters.get("search_text")
    context_lines = parameters.get("context_lines", 5)
    stream = parameters.get("stream", False)
    chunk_size = parameters.get("chunk_size", 8192)
    max_lines = parameters.get("max_lines", 1000)
    encoding = parameters.get("encoding")
    detect_encoding = parameters.get("detect_encoding", True)
    
    if not file_path:
        return "❌ 错误：缺少文件路径"
    
    # 自动检测编码
    if detect_encoding and not encoding:
        encoding = _detect_file_encoding(file_path)
    
    # 流式读取大文件
    if stream:
        return _read_file_stream(file_path, chunk_size, encoding, max_lines)
    
    # 普通读取
    return _read_file_normal(file_path, search_text, context_lines, encoding, max_lines)


def _detect_file_encoding(file_path: str) -> str:
    """检测文件编码"""
    try:
        with open(file_path, 'rb') as f:
            raw = f.read(1024 * 1024)  # 读取前 1MB
            result = chardet.detect(raw)
            encoding = result['encoding']
            confidence = result['confidence']
            
            # 置信度>0.7 才使用检测结果
            if confidence > 0.7 and encoding:
                return encoding
    except Exception:
        pass
    
    return 'utf-8'  # 默认 UTF-8


def _read_file_stream(file_path: str, chunk_size: int, encoding: str, max_lines: int) -> str:
    """流式读取大文件"""
    lines_count = 0
    content_chunks = []
    
    try:
        with open(file_path, 'r', encoding=encoding) as f:
            while True:
                chunk = f.read(chunk_size)
                if not chunk:
                    break
                
                content_chunks.append(chunk)
                lines_count += chunk.count('\n')
                
                # 超过最大行数限制
                if lines_count > max_lines:
                    return f"⚠️ 文件过大，已读取前{max_lines}行（共{len(''.join(content_chunks))}字节）\n\n" + ''.join(content_chunks)
        
        return f"✅ 文件读取成功（{lines_count}行，{len(''.join(content_chunks))}字节）\n\n" + ''.join(content_chunks)
    
    except UnicodeDecodeError as e:
        return f"❌ 编码错误：{e}\n提示：尝试设置 encoding 参数或使用 detect_encoding=true"
    except Exception as e:
        return f"❌ 读取失败：{e}"


def _read_file_normal(file_path: str, search_text: str, context_lines: int, encoding: str, max_lines: int) -> str:
    """普通读取（带搜索功能）"""
    try:
        with open(file_path, 'r', encoding=encoding) as f:
            lines = f.readlines()
        
        # 限制最大行数
        if len(lines) > max_lines:
            lines = lines[:max_lines]
            truncated_msg = f"\n\n⚠️ 文件过大，已截断至前{max_lines}行"
        else:
            truncated_msg = ""
        
        if not search_text:
            return f"📄 文件内容 ({len(lines)}行):\n\n{''.join(lines)}{truncated_msg}"
        
        # 搜索功能
        search_text_lower = search_text.lower()
        results = []
        
        for i, line in enumerate(lines, 1):
            if search_text_lower in line.lower():
                start = max(0, i - context_lines - 1)
                end = min(len(lines), i + context_lines)
                context = ''.join(lines[start:end])
                results.append(f"--- 第 {i} 行 ---\n{context}")
        
        if not results:
            return f"🔍 未找到搜索文本：{search_text}{truncated_msg}"
        
        return f"🔍 搜索结果 (共{len(results)}处匹配):\n\n" + "\n\n".join(results) + truncated_msg
    
    except Exception as e:
        return f"❌ 读取失败：{e}"
```

**SKILL.md 更新：**
```markdown
---
name: read_file
description: "读取文件内容，支持搜索、流式读取、编码自动检测"
version: "2.0.0"
trigger_conditions: "用户请求读取文件、查看文件内容、搜索文件中的文本时触发"
execution_guidelines: "1. 验证 file_path 参数\n2. 大文件自动使用流式读取\n3. 自动检测文件编码\n4. 搜索时显示上下文"
author: "Lingxi Team"
license: MIT
---

# Read File Skill v2.0

## 新增功能

- ✅ **流式读取** - 支持大文件分块读取，避免内存溢出
- ✅ **编码检测** - 自动检测文件编码（支持 UTF-8、GBK、Big5 等）
- ✅ **行数限制** - 默认限制 1000 行，防止输出过大
- ✅ **搜索增强** - 支持不区分大小写搜索

## 参数说明

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| **file_path** | string | ✅ | - | 文件绝对路径 |
| **search_text** | string | ❌ | - | 搜索关键词 |
| **context_lines** | number | ❌ | 5 | 搜索上下文行数 |
| **stream** | boolean | ❌ | false | 是否流式读取 |
| **chunk_size** | number | ❌ | 8192 | 流式读取块大小（字节） |
| **max_lines** | number | ❌ | 1000 | 最大读取行数 |
| **encoding** | string | ❌ | - | 文件编码（可选） |
| **detect_encoding** | boolean | ❌ | true | 自动检测编码 |

## 使用示例

```python
# 读取小文件
read_file(file_path="/path/to/file.txt")

# 搜索文件内容
read_file(
    file_path="/path/to/file.txt",
    search_text="Python",
    context_lines=3
)

# 流式读取大文件
read_file(
    file_path="/path/to/large.log",
    stream=true,
    chunk_size=16384,
    max_lines=5000
)

# 指定编码读取
read_file(
    file_path="/path/to/gbk_file.txt",
    encoding="gbk",
    detect_encoding=false
)
```

## 依赖

- `chardet` - 编码检测库
```

---

### 方案 2：create_file 技能增强

#### 2.1 添加追加模式

**新增参数：**
```json
{
    "file_path": "/path/to/file.txt",
    "content": "文件内容",
    "mode": "write",           // 新增：write 或 append
    "create_parent_dirs": true, // 新增：自动创建父目录
    "encoding": "utf-8"        // 新增：编码
}
```

**实现代码：**
```python
#!/usr/bin/env python3
"""Create file skill - 增强版"""

import os
from typing import Dict, Any


def execute(parameters: Dict[str, Any]) -> str:
    file_path = parameters.get("file_path")
    content = parameters.get("content", "")
    mode = parameters.get("mode", "write")  # "write" or "append"
    create_parent_dirs = parameters.get("create_parent_dirs", True)
    encoding = parameters.get("encoding", "utf-8")
    
    if not file_path:
        return "❌ 错误：缺少文件路径"
    
    try:
        # 自动创建父目录
        if create_parent_dirs:
            dir_path = os.path.dirname(file_path)
            if dir_path and not os.path.exists(dir_path):
                os.makedirs(dir_path, exist_ok=True)
        
        # 选择写入模式
        file_mode = 'a' if mode == 'append' else 'w'
        
        # 写入文件
        with open(file_path, file_mode, encoding=encoding) as f:
            f.write(content)
        
        # 获取文件大小
        file_size = os.path.getsize(file_path)
        
        if mode == 'append':
            return f"✅ 文件追加成功：{file_path}\n追加大小：{len(content)}字节\n当前文件大小：{file_size}字节"
        else:
            return f"✅ 文件创建成功：{file_path}\n文件大小：{len(content)}字节"
    
    except Exception as e:
        return f"❌ 创建失败：{e}"
```

---

### 方案 3：新增 apply_patch 技能

#### 3.1 Diff 补丁应用

**创建新技能：** `skills/builtin/apply_patch/`

**main.py：**
```python
#!/usr/bin/env python3
"""Apply patch skill - 应用统一 diff 格式补丁"""

import os
import logging
from typing import Dict, Any, List, Tuple
from difflib import unified_diff

logger = logging.getLogger(__name__)


def execute(parameters: Dict[str, Any]) -> str:
    """
    应用 diff 补丁到文件
    
    参数：
    - file_path: 目标文件路径（必填）
    - patch_text: 统一 diff 格式补丁（必填）
    - dry_run: 是否仅预览不应用（可选，默认 false）
    - backup: 是否创建备份（可选，默认 true）
    """
    file_path = parameters.get("file_path")
    patch_text = parameters.get("patch_text")
    dry_run = parameters.get("dry_run", False)
    backup = parameters.get("backup", True)
    
    if not file_path:
        return "❌ 错误：缺少 file_path 参数"
    
    if not patch_text:
        return "❌ 错误：缺少 patch_text 参数"
    
    if not os.path.exists(file_path):
        return f"❌ 错误：文件不存在 - {file_path}"
    
    try:
        # 解析补丁
        changes = _parse_unified_diff(patch_text)
        
        if not changes:
            return "❌ 错误：无法解析补丁，请确保是统一 diff 格式"
        
        # 读取原文件
        with open(file_path, 'r', encoding='utf-8') as f:
            original_lines = f.readlines()
        
        # 应用补丁
        modified_lines = _apply_changes(original_lines, changes)
        
        if dry_run:
            # 预览模式：显示 diff
            diff = list(unified_diff(
                original_lines,
                modified_lines,
                fromfile='a/' + os.path.basename(file_path),
                tofile='b/' + os.path.basename(file_path)
            ))
            return "🔍 预览模式（未应用补丁）:\n\n" + ''.join(diff)
        
        # 创建备份
        if backup:
            backup_path = file_path + '.bak'
            with open(backup_path, 'w', encoding='utf-8') as f:
                f.writelines(original_lines)
        
        # 写回文件
        with open(file_path, 'w', encoding='utf-8') as f:
            f.writelines(modified_lines)
        
        return f"✅ 补丁应用成功：{file_path}\n修改行数：{len(changes)}\n备份文件：{backup_path if backup else '未创建'}"
    
    except Exception as e:
        logger.error(f"应用补丁失败：{e}")
        return f"❌ 应用失败：{e}"


def _parse_unified_diff(patch_text: str) -> List[Dict[str, Any]]:
    """
    解析统一 diff 格式
    
    返回变更列表：
    [
        {
            "operation": "add" | "remove" | "replace",
            "line_number": 10,
            "old_text": "old line",
            "new_text": "new line"
        }
    ]
    """
    changes = []
    current_line = 0
    
    for line in patch_text.split('\n'):
        if line.startswith('@@'):
            # 解析行号 @@ -10,3 +10,4 @@
            parts = line.split()
            if len(parts) >= 3:
                old_range = parts[1]  # -10,3
                current_line = int(old_range.split(',')[0].replace('-', ''))
        
        elif line.startswith('+') and not line.startswith('+++'):
            # 新增行
            changes.append({
                "operation": "add",
                "line_number": current_line,
                "new_text": line[1:]
            })
            current_line += 1
        
        elif line.startswith('-') and not line.startswith('---'):
            # 删除行
            changes.append({
                "operation": "remove",
                "line_number": current_line,
                "old_text": line[1:]
            })
        
        elif line.startswith(' '):
            # 上下文（未变更）
            current_line += 1
    
    return changes


def _apply_changes(original_lines: List[str], changes: List[Dict[str, Any]]) -> List[str]:
    """应用变更到原文件"""
    # 简单实现：按顺序应用变更
    # TODO: 实现更智能的变更应用逻辑
    
    modified_lines = original_lines.copy()
    offset = 0
    
    for change in changes:
        line_idx = change['line_number'] - 1 + offset
        
        if change['operation'] == 'add':
            modified_lines.insert(line_idx + 1, change['new_text'] + '\n')
            offset += 1
        
        elif change['operation'] == 'remove':
            if 0 <= line_idx < len(modified_lines):
                modified_lines.pop(line_idx)
                offset -= 1
    
    return modified_lines
```

**SKILL.md：**
```markdown
---
name: apply_patch
description: "应用统一 diff 格式补丁到文件，支持预览和备份"
version: "1.0.0"
trigger_conditions: "用户请求应用补丁、应用 diff、代码重构时触发"
execution_guidelines: "1. 验证 patch_text 格式\n2. 默认创建备份\n3. 支持 dry_run 预览"
author: "Lingxi Team"
license: MIT
---

# Apply Patch Skill

应用统一 diff 格式补丁到目标文件。

## 参数说明

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| **file_path** | string | ✅ | - | 目标文件路径 |
| **patch_text** | string | ✅ | - | 统一 diff 格式补丁 |
| **dry_run** | boolean | ❌ | false | 仅预览不应用 |
| **backup** | boolean | ❌ | true | 创建备份文件 |

## 使用示例

```python
# 应用补丁
apply_patch(
    file_path="/path/to/file.py",
    patch_text="""--- a/file.py
+++ b/file.py
@@ -10,3 +10,4 @@
 line 10
-old line 11
+new line 11
 line 12
+new line 13
"""
)

# 预览补丁（不应用）
apply_patch(
    file_path="/path/to/file.py",
    patch_text="...",
    dry_run=true
)

# 应用补丁但不创建备份
apply_patch(
    file_path="/path/to/file.py",
    patch_text="...",
    backup=false
)
```

## Diff 格式

支持标准统一 diff 格式：

```diff
--- a/file.py
+++ b/file.py
@@ -10,3 +10,4 @@
 context line
-removed line
+added line
 context line
```
```

---

### 方案 4：新增 batch_read 技能

#### 4.1 批量读取文件

**创建新技能：** `skills/builtin/batch_read/`

**main.py：**
```python
#!/usr/bin/env python3
"""Batch read files skill - 批量读取多个文件"""

import os
import glob
from typing import Dict, Any, List


def execute(parameters: Dict[str, Any]) -> str:
    """
    批量读取文件
    
    参数：
    - file_paths: 文件路径列表（必填）
    - pattern: 文件匹配模式（可选，如 "*.py"）
    - directory: 搜索目录（可选，与 pattern 配合使用）
    - max_files: 最大文件数（可选，默认 20）
    - max_size_per_file: 单个文件最大字节数（可选，默认 100KB）
    """
    file_paths = parameters.get("file_paths", [])
    pattern = parameters.get("pattern")
    directory = parameters.get("directory")
    max_files = parameters.get("max_files", 20)
    max_size_per_file = parameters.get("max_size_per_file", 100 * 1024)
    
    # 使用 pattern 搜索文件
    if pattern and directory:
        search_pattern = os.path.join(directory, "**", pattern)
        file_paths = glob.glob(search_pattern, recursive=True)
        file_paths = file_paths[:max_files]
    
    if not file_paths:
        return "❌ 错误：没有指定文件路径"
    
    # 批量读取
    results = []
    total_size = 0
    total_lines = 0
    
    for file_path in file_paths[:max_files]:
        try:
            # 检查文件大小
            file_size = os.path.getsize(file_path)
            if file_size > max_size_per_file:
                results.append({
                    "path": file_path,
                    "error": f"文件过大 ({file_size}字节 > {max_size_per_file}字节限制)"
                })
                continue
            
            # 读取文件
            with open(file_path, 'r', encoding='utf-8') as f:
                content = f.read()
            
            lines = content.count('\n') + 1
            results.append({
                "path": file_path,
                "content": content,
                "size": file_size,
                "lines": lines,
                "success": True
            })
            
            total_size += file_size
            total_lines += lines
        
        except Exception as e:
            results.append({
                "path": file_path,
                "error": str(e),
                "success": False
            })
    
    # 生成报告
    success_count = sum(1 for r in results if r.get("success"))
    failed_count = len(results) - success_count
    
    report = f"📦 批量读取完成\n\n"
    report += f"✅ 成功：{success_count}个\n"
    report += f"❌ 失败：{failed_count}个\n"
    report += f"📊 总计：{total_size}字节，{total_lines}行\n\n"
    
    # 显示内容
    for result in results:
        report += f"{'='*60}\n"
        report += f"📄 {result['path']}\n"
        report += f"{'='*60}\n"
        
        if result.get("success"):
            report += f"大小：{result['size']}字节 | 行数：{result['lines']}\n\n"
            report += result.get("content", "")[:2000]  # 限制显示长度
            if len(result.get("content", "")) > 2000:
                report += "\n...（内容已截断）"
        else:
            report += f"❌ 错误：{result.get('error')}\n"
        
        report += "\n\n"
    
    return report
```

**SKILL.md：**
```markdown
---
name: batch_read
description: "批量读取多个文件，支持通配符匹配和大小限制"
version: "1.0.0"
trigger_conditions: "用户请求读取多个文件、查看项目结构、批量分析代码时触发"
execution_guidelines: "1. 验证文件路径\n2. 限制文件大小\n3. 限制文件数量"
author: "Lingxi Team"
license: MIT
---

# Batch Read Skill

批量读取多个文件内容。

## 参数说明

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| **file_paths** | string[] | ❌ | [] | 文件路径列表 |
| **pattern** | string | ❌ | - | 文件匹配模式（如 "*.py"） |
| **directory** | string | ❌ | - | 搜索目录 |
| **max_files** | number | ❌ | 20 | 最大文件数 |
| **max_size_per_file** | number | ❌ | 102400 | 单文件最大字节数 |

## 使用示例

```python
# 读取指定文件列表
batch_read(
    file_paths=[
        "/path/to/file1.py",
        "/path/to/file2.py",
        "/path/to/file3.py"
    ]
)

# 使用通配符读取目录下所有 Python 文件
batch_read(
    pattern="*.py",
    directory="/path/to/project",
    max_files=10
)

# 读取配置文件
batch_read(
    pattern="*.json",
    directory="/path/to/config",
    max_size_per_file=51200
)
```
```

---

## 📋 实现计划

### Phase 1：基础增强（1 天）
- [ ] read_file 流式读取
- [ ] read_file 编码自动检测
- [ ] create_file 追加模式
- [ ] 安装 chardet 依赖

### Phase 2：新技能开发（2 天）
- [ ] apply_patch 技能
- [ ] batch_read 技能
- [ ] 编写单元测试

### Phase 3：优化与测试（1 天）
- [ ] 性能优化
- [ ] 错误处理完善
- [ ] 文档更新
- [ ] 集成测试

---

## 🧪 测试用例

### read_file 测试
```python
# 测试流式读取
def test_stream_read():
    result = execute({
        "file_path": "/path/to/large.txt",
        "stream": True,
        "max_lines": 100
    })
    assert "已读取" in result

# 测试编码检测
def test_encoding_detection():
    result = execute({
        "file_path": "/path/to/gbk_file.txt",
        "detect_encoding": True
    })
    assert "❌" not in result

# 测试搜索功能
def test_search():
    result = execute({
        "file_path": "/path/to/file.txt",
        "search_text": "keyword",
        "context_lines": 3
    })
    assert "搜索结果" in result or "未找到" in result
```

### apply_patch 测试
```python
def test_apply_patch():
    result = execute({
        "file_path": "/path/to/file.py",
        "patch_text": unified_diff,
        "dry_run": True
    })
    assert "预览模式" in result

def test_apply_patch_backup():
    result = execute({
        "file_path": "/path/to/file.py",
        "patch_text": unified_diff,
        "backup": True
    })
    assert "备份文件" in result
```

### batch_read 测试
```python
def test_batch_read():
    result = execute({
        "file_paths": ["/path/to/file1.txt", "/path/to/file2.txt"],
        "max_files": 10
    })
    assert "批量读取完成" in result
    assert "✅ 成功" in result
```

---

## 📊 依赖更新

**requirements.txt 新增：**
```
chardet>=5.0.0  # 编码检测
```

**安装命令：**
```bash
cd /home/admin/lingxi-assistant/lingxi
/usr/local/bin/python3.10 -m pip install chardet
```

---

## 🎯 验收标准

| 功能 | 验收标准 | 状态 |
|------|----------|------|
| 流式读取 | 100MB 文件读取<5 秒，内存<50MB | ⏳ |
| 编码检测 | GBK/UTF-8/Big5 准确率>95% | ⏳ |
| 追加模式 | 追加内容正确，文件大小正确 | ⏳ |
| Diff 应用 | 标准 diff 格式解析成功率>90% | ⏳ |
| 批量读取 | 20 个文件并发读取<10 秒 | ⏳ |
| 单元测试 | 覆盖率>80% | ⏳ |

---

**设计完成！需要开始实现吗？** 🚀
