# 工作目录功能详细设计（V4.0）

**文档版本**: V1.0  
**创建日期**: 2026-03-07  
**适用系统**: 灵犀个人智能助手（桌面版）  
**优先级**: P0（核心功能）

---

## 一、需求概述

### 1.1 核心目标

为灵犀智能助手实现**工作目录（Workspace）**管理机制，支持：

- ✅ Agent 在指定的工作目录下安全工作（文件操作、命令执行）
- ✅ 工作目录初始化（创建 `.lingxi` 目录结构）
- ✅ 运行时动态切换工作目录
- ✅ 工作目录级别的配置、数据库、技能隔离

### 1.2 使用场景

| 场景 | 说明 |
|------|------|
| 启动时指定 | 使用启动命令所在目录作为工作目录 |
| 桌面版切换 | 用户通过桌面客户端切换到不同项目目录 |
| 多项目管理 | 每个项目有独立的 `.lingxi` 配置、数据库、技能 |

### 1.3 设计原则

- **安全隔离**：Agent 只能在指定工作目录内操作，防止跨目录访问
- **配置优先**：工作目录配置优先级高于全局配置
- **技能隔离**：支持工作目录级别的自定义技能
- **平滑切换**：切换工作目录时等待任务完成，保存会话状态

---

## 二、目录结构设计

### 2.1 工作目录结构

```
/workspace-root/              # 工作目录根目录
├── .lingxi/                  # 灵犀专属目录（隐藏）
│   ├── conf/
│   │   └── config.yml       # 工作目录配置（覆盖全局配置）
│   ├── data/
│   │   ├── assistant.db     # 会话数据库（运行时创建）
│   │   └── long_term_memory.db  # 长期记忆数据库（运行时创建）
│   └── skills/              # 工作目录级别技能
│       ├── my_skill_1/
│       └── my_skill_2/
├── src/                     # 用户业务文件（不受管）
├── data/
└── 其它用户文件...
```

### 2.2 目录说明

| 目录/文件 | 说明 | 创建时机 |
|----------|------|---------|
| `.lingxi/` | 灵犀专属目录，隐藏 | 初始化时创建 |
| `.lingxi/conf/config.yml` | 工作目录配置文件 | 初始化时创建（如不存在） |
| `.lingxi/data/` | 数据目录 | 初始化时创建 |
| `.lingxi/data/assistant.db` | 会话数据库（sessions/tasks/steps） | 运行时动态创建 |
| `.lingxi/data/long_term_memory.db` | 长期记忆数据库 | 运行时动态创建 |
| `.lingxi/skills/` | 工作目录级别自定义技能 | 用户手动添加 |
| 根目录其它文件 | 用户业务文件 | 用户创建，保持原样 |

### 2.3 `.lingxi/conf/config.yml` 配置模板

```yaml
# 工作目录配置（优先级高于全局配置）
workspace:
  name: "默认工作空间"
  description: "灵犀助手工作目录"

# 技能配置（工作目录级别）
skills:
  enabled:
    - "custom_skill_1"
    - "custom_skill_2"

# 数据库配置
database:
  assistant_db: "./data/assistant.db"
  memory_db: "./data/long_term_memory.db"

# 安全配置（可覆盖全局）
security:
  safety_mode: true
  max_file_size: 10485760
  allowed_commands:
    - "ls"
    - "pwd"
    - "git"
    - "cat"
    - "grep"
```

---

## 三、架构设计

### 3.1 组件关系图

```
┌─────────────────────────────────────────────────────────┐
│                   WorkspaceManager                      │
│  (lingxi/management/workspace.py)                       │
├─────────────────────────────────────────────────────────┤
│  - current_workspace: Path                              │
│  - lingxi_config: Dict                                  │
│  - sandbox: SecuritySandbox                             │
│  - skill_caller: SkillCaller                            │
│  - session_store: SessionStore                          │
│  - event_publisher: EventPublisher                      │
└─────────────────────────────────────────────────────────┘
           │              │              │
           │              │              │
           ▼              ▼              ▼
┌─────────────────┐ ┌──────────────┐ ┌──────────────┐
│ SecuritySandbox │ │ SkillRegistry│ │ SessionStore │
│  (core/security)│ │ (skills/reg) │ │ (db/store)   │
└─────────────────┘ └──────────────┘ └──────────────┘
```

### 3.2 核心组件职责

| 组件 | 文件位置 | 职责 |
|------|---------|------|
| **WorkspaceManager** | `lingxi/management/workspace.py` | 工作目录生命周期管理（切换、初始化、资源加载） |
| **WorkspaceInitializer** | `lingxi/management/workspace.py` | 初始化 `.lingxi` 目录结构 |
| **WorkspaceConfig** | `lingxi/management/workspace.py` | 加载/合并工作目录配置 |
| **SecuritySandbox** | `lingxi/core/security.py` | 文件操作/命令执行限制在工作目录内 |
| **SkillRegistry** | `lingxi/skills/registry.py` | 支持工作目录技能优先加载 |
| **SessionStore** | `lingxi/db/session_store.py` | 工作目录数据库连接管理 |

### 3.3 配置优先级

```
工作目录配置 (.lingxi/conf/config.yml)
    ↓ 覆盖
全局配置 (config.yaml)
    ↓ 使用
默认配置 (代码硬编码)
```

---

## 四、详细设计

### 4.1 WorkspaceManager 核心方法

**文件位置**: `lingxi/management/workspace.py`

```python
"""工作目录管理模块

提供工作目录初始化、切换、资源配置管理功能
"""

import os
import yaml
import shutil
from pathlib import Path
from typing import Dict, Any, Optional, List
from datetime import datetime
import logging

from lingxi.core.security import SecuritySandbox
from lingxi.skills.registry import SkillRegistry
from lingxi.db.session_store import SessionStore
from lingxi.core.event import EventPublisher


class WorkspaceError(Exception):
    """工作目录异常基类"""
    pass


class WorkspaceNotFoundError(WorkspaceError):
    """工作目录不存在"""
    pass


class WorkspaceInitError(WorkspaceError):
    """工作目录初始化失败"""
    pass


class WorkspaceSwitchError(WorkspaceError):
    """工作目录切换失败"""
    pass


class WorkspaceManager:
    """工作目录管理器（事件驱动 + 资源管理器模式）"""
    
    def __init__(self, config: Dict[str, Any]):
        """初始化工作目录管理器
        
        Args:
            config: 全局配置文件
        """
        self.config = config
        self.current_workspace: Optional[Path] = None
        self.previous_workspace: Optional[Path] = None
        self.lingxi_config: Optional[Dict[str, Any]] = None
        
        # 资源引用（切换时更新）
        self.sandbox: Optional[SecuritySandbox] = None
        self.skill_caller: Optional[Any] = None
        self.session_store: Optional[SessionStore] = None
        self.event_publisher: Optional[EventPublisher] = None
        
        self.logger = logging.getLogger(__name__)
    
    def initialize(self, workspace_path: Optional[str] = None) -> Path:
        """初始化工作目录
        
        Args:
            workspace_path: 工作目录路径（None 则使用当前目录）
        
        Returns:
            .lingxi 目录路径
        """
        # 1. 确定工作目录
        if workspace_path is None:
            workspace_path = os.getcwd()
        
        workspace_path = Path(workspace_path).resolve()
        
        # 2. 初始化 .lingxi 目录结构
        lingxi_dir = self._initialize_lingxi_directory(workspace_path)
        
        # 3. 加载配置
        self.lingxi_config = self._load_workspace_config(lingxi_dir)
        
        # 4. 设置当前工作目录
        self.current_workspace = workspace_path
        
        # 5. 发布事件
        if self.event_publisher:
            self.event_publisher.publish("workspace_initialized", {
                "workspace": str(workspace_path),
                "lingxi_dir": str(lingxi_dir)
            })
        
        self.logger.info(f"工作目录初始化完成：{workspace_path}")
        return lingxi_dir
    
    async def switch_workspace(self, workspace_path: str, force: bool = False) -> Dict[str, Any]:
        """切换工作目录（等待任务完成）
        
        Args:
            workspace_path: 新的工作目录路径
            force: 是否强制切换（忽略执行中任务）
        
        Returns:
            切换结果
        """
        self.previous_workspace = self.current_workspace
        
        # 1. 检查是否有执行中的任务
        if not force and self._has_running_tasks():
            self.logger.info("等待任务完成...")
            await self._wait_for_tasks_completion(timeout=300)  # 5 分钟超时
        
        # 2. 验证路径有效性
        workspace_path = Path(workspace_path).resolve()
        if not workspace_path.exists():
            raise WorkspaceNotFoundError(f"工作目录不存在：{workspace_path}")
        
        # 3. 保存当前会话状态
        self._save_session_state()
        
        # 4. 卸载当前资源
        self._unload_resources()
        
        # 5. 初始化/加载新工作目录
        lingxi_dir = self._initialize_lingxi_directory(workspace_path)
        self.lingxi_config = self._load_workspace_config(lingxi_dir)
        
        # 6. 加载新资源
        self._load_resources(workspace_path, lingxi_dir)
        
        # 7. 更新当前工作目录
        self.current_workspace = workspace_path
        
        # 8. 持久化配置
        self._persist_workspace_path(workspace_path)
        
        # 9. 发布事件
        if self.event_publisher:
            self.event_publisher.publish("workspace_switched", {
                "previous_workspace": str(self.previous_workspace),
                "current_workspace": str(workspace_path),
                "lingxi_dir": str(lingxi_dir),
                "switched_at": datetime.now().isoformat()
            })
        
        self.logger.info(f"工作目录已切换：{workspace_path}")
        
        return {
            "success": True,
            "data": {
                "previous_workspace": str(self.previous_workspace),
                "current_workspace": str(workspace_path),
                "lingxi_dir": str(lingxi_dir),
                "switched_at": datetime.now().isoformat()
            }
        }
    
    def _initialize_lingxi_directory(self, workspace_path: Path) -> Path:
        """初始化 .lingxi 目录结构
        
        目录结构：
        .lingxi/
        ├── conf/
        │   └── config.yml
        ├── data/
        └── skills/
        
        Args:
            workspace_path: 工作目录路径
        
        Returns:
            .lingxi 目录路径
        """
        lingxi_dir = workspace_path / ".lingxi"
        
        # 如果已存在，直接返回
        if lingxi_dir.exists():
            self.logger.debug(f".lingxi 目录已存在：{lingxi_dir}")
            return lingxi_dir
        
        # 创建目录结构
        (lingxi_dir / "conf").mkdir(parents=True, exist_ok=True)
        (lingxi_dir / "data").mkdir(parents=True, exist_ok=True)
        (lingxi_dir / "skills").mkdir(parents=True, exist_ok=True)
        
        # 创建默认配置文件（如果不存在）
        config_file = lingxi_dir / "conf" / "config.yml"
        if not config_file.exists():
            self._create_default_workspace_config(config_file)
        
        self.logger.debug(f".lingxi 目录初始化完成：{lingxi_dir}")
        return lingxi_dir
    
    def _create_default_workspace_config(self, config_file: Path):
        """创建默认工作目录配置文件
        
        Args:
            config_file: 配置文件路径
        """
        default_config = {
            "workspace": {
                "name": "默认工作空间",
                "description": "灵犀助手工作目录"
            },
            "skills": {
                "enabled": []
            },
            "database": {
                "assistant_db": "./data/assistant.db",
                "memory_db": "./data/long_term_memory.db"
            },
            "security": {
                "safety_mode": True,
                "max_file_size": 10485760
            }
        }
        
        with open(config_file, 'w', encoding='utf-8') as f:
            yaml.dump(default_config, f, allow_unicode=True, default_flow_style=False)
        
        self.logger.debug(f"创建默认配置文件：{config_file}")
    
    def _load_workspace_config(self, lingxi_dir: Path) -> Dict[str, Any]:
        """加载工作目录配置（优先级高于全局配置）
        
        Args:
            lingxi_dir: .lingxi 目录路径
        
        Returns:
            合并后的配置
        """
        config_file = lingxi_dir / "conf" / "config.yml"
        
        if not config_file.exists():
            return self.config
        
        with open(config_file, 'r', encoding='utf-8') as f:
            workspace_config = yaml.safe_load(f)
        
        # 合并配置（工作目录配置覆盖全局配置）
        merged_config = self._deep_merge(self.config, workspace_config)
        
        self.logger.debug(f"工作目录配置已加载：{config_file}")
        return merged_config
    
    def _deep_merge(self, base: Dict, override: Dict) -> Dict:
        """深度合并字典（override 优先）
        
        Args:
            base: 基础字典
            override: 覆盖字典
        
        Returns:
            合并后的字典
        """
        result = base.copy()
        
        for key, value in override.items():
            if isinstance(value, dict) and key in result:
                result[key] = self._deep_merge(result[key], value)
            else:
                result[key] = value
        
        return result
    
    def _unload_resources(self):
        """卸载当前工作目录的资源"""
        self.logger.info("开始卸载资源...")
        
        # 1. 关闭数据库连接
        if self.session_store:
            self.session_store.close()
            self.logger.debug("数据库连接已关闭")
        
        # 2. 注销工作目录级别的技能
        if self.skill_caller and hasattr(self.skill_caller, 'registry'):
            self._unregister_workspace_skills()
            self.logger.debug("工作目录技能已注销")
        
        self.logger.info("资源卸载完成")
    
    def _load_resources(self, workspace_path: Path, lingxi_dir: Path):
        """加载新工作目录的资源
        
        Args:
            workspace_path: 工作目录路径
            lingxi_dir: .lingxi 目录路径
        """
        self.logger.info("开始加载资源...")
        
        # 1. 更新 SecuritySandbox 的 workspace_root
        if self.sandbox:
            self.sandbox.update_workspace(workspace_path)
            self.logger.debug("SecuritySandbox 工作目录已更新")
        
        # 2. 重新加载技能注册表（工作目录技能优先）
        if self.skill_caller and hasattr(self.skill_caller, 'registry'):
            self._register_workspace_skills(lingxi_dir / "skills")
            self.logger.debug("工作目录技能已注册")
        
        # 3. 重新初始化数据库连接
        if self.session_store:
            self._initialize_database(lingxi_dir / "data")
            self.logger.debug("数据库连接已初始化")
        
        self.logger.info("资源加载完成")
    
    def _register_workspace_skills(self, skills_dir: Path):
        """注册工作目录级别的技能
        
        Args:
            skills_dir: 技能目录
        """
        if not skills_dir.exists():
            return
        
        registry = self.skill_caller.registry
        
        # 先注销旧的工作目录技能
        self._unregister_workspace_skills()
        
        # 注册新技能
        for skill_dir in skills_dir.iterdir():
            if skill_dir.is_dir() and (skill_dir / "main.py").exists():
                try:
                    registry.register_skill_from_dir(skill_dir, source="workspace")
                    self.logger.info(f"注册工作目录技能：{skill_dir.name}")
                except Exception as e:
                    self.logger.error(f"注册技能失败 {skill_dir.name}: {e}")
    
    def _unregister_workspace_skills(self):
        """注销工作目录级别的技能"""
        if not hasattr(self.skill_caller, 'registry'):
            return
        
        registry = self.skill_caller.registry
        workspace_skills = [
            name for name, info in registry.skills.items()
            if info.get("source") == "workspace"
        ]
        
        for skill_name in workspace_skills:
            del registry.skills[skill_name]
            self.logger.debug(f"已注销工作目录技能：{skill_name}")
    
    def _initialize_database(self, data_dir: Path):
        """初始化数据库连接
        
        Args:
            data_dir: 数据目录
        """
        if not self.session_store:
            return
        
        # 更新数据库路径
        assistant_db = data_dir / "assistant.db"
        memory_db = data_dir / "long_term_memory.db"
        
        # 重新初始化连接
        self.session_store.initialize_database(assistant_db, memory_db)
    
    def _has_running_tasks(self) -> bool:
        """检查是否有执行中的任务
        
        Returns:
            是否有执行中的任务
        """
        # TODO: 实现任务检查逻辑
        # 可以通过检查执行引擎的状态
        return False
    
    async def _wait_for_tasks_completion(self, timeout: int = 300):
        """等待任务完成
        
        Args:
            timeout: 超时时间（秒）
        """
        # TODO: 实现等待逻辑
        # 可以轮询任务状态，或监听任务完成事件
        import asyncio
        start_time = datetime.now()
        
        while self._has_running_tasks():
            if (datetime.now() - start_time).total_seconds() > timeout:
                self.logger.warning("等待任务完成超时")
                break
            await asyncio.sleep(1)
    
    def _save_session_state(self):
        """保存当前会话状态"""
        # TODO: 实现会话状态保存逻辑
        self.logger.debug("会话状态已保存")
    
    def _persist_workspace_path(self, workspace_path: Path):
        """持久化工作目录路径到全局配置
        
        Args:
            workspace_path: 工作目录路径
        """
        config_file = Path(__file__).parent.parent / "config.yaml"
        
        if not config_file.exists():
            self.logger.warning(f"配置文件不存在：{config_file}")
            return
        
        try:
            with open(config_file, 'r', encoding='utf-8') as f:
                config = yaml.safe_load(f)
            
            if 'workspace' not in config:
                config['workspace'] = {}
            
            config['workspace']['last_workspace'] = str(workspace_path)
            
            with open(config_file, 'w', encoding='utf-8') as f:
                yaml.dump(config, f, allow_unicode=True, default_flow_style=False)
            
            self.logger.debug(f"工作目录已持久化：{workspace_path}")
        except Exception as e:
            self.logger.error(f"持久化配置失败：{e}")
    
    def get_current_workspace(self) -> Optional[Path]:
        """获取当前工作目录
        
        Returns:
            当前工作目录路径
        """
        return self.current_workspace
    
    def get_lingxi_directory(self) -> Optional[Path]:
        """获取 .lingxi 目录路径
        
        Returns:
            .lingxi 目录路径
        """
        if self.current_workspace:
            return self.current_workspace / ".lingxi"
        return None
    
    def validate_workspace(self, workspace_path: Path) -> bool:
        """验证工作目录是否有效
        
        Args:
            workspace_path: 工作目录路径
        
        Returns:
            是否有效
        """
        # 检查目录是否存在
        if not workspace_path.exists():
            return False
        
        # 检查是否有 .lingxi 目录（没有就创建）
        lingxi_dir = workspace_path / ".lingxi"
        if not lingxi_dir.exists():
            self.logger.info(f"工作目录缺少 .lingxi 子目录，将自动创建：{lingxi_dir}")
        
        return True
```

---

### 4.2 SecuritySandbox 扩展

**修改文件**: `lingxi/core/security.py`

**新增方法**:

```python
class SecuritySandbox:
    # ... 现有代码 ...
    
    def update_workspace(self, new_workspace: Path):
        """更新工作目录根目录
        
        Args:
            new_workspace: 新的工作目录路径
        """
        old_workspace = self.workspace_root
        self.workspace_root = Path(new_workspace).resolve()
        self.workspace_root.mkdir(parents=True, exist_ok=True)
        
        self.logger.info(f"工作目录已更新：{old_workspace} -> {self.workspace_root}")
    
    def get_workspace(self) -> Path:
        """获取当前工作目录"""
        return self.workspace_root
```

---

### 4.3 技能注册表扩展

**修改文件**: `lingxi/skills/registry.py`

**新增方法**:

```python
class SkillRegistry:
    # ... 现有代码 ...
    
    def register_skill_from_dir(self, skill_dir: Path, source: str = "global"):
        """从目录注册技能
        
        Args:
            skill_dir: 技能目录
            source: 技能来源（global/workspace）
        """
        if not (skill_dir / "main.py").exists():
            raise ValueError(f"技能目录缺少 main.py: {skill_dir}")
        
        # 加载技能模块
        spec = importlib.util.spec_from_file_location(
            f"skill_{skill_dir.name}",
            skill_dir / "main.py"
        )
        module = importlib.util.module_from_spec(spec)
        spec.loader.exec_module(module)
        
        # 获取技能信息
        if hasattr(module, 'get_skill_info'):
            skill_info = module.get_skill_info()
            skill_info['source'] = source
            skill_info['path'] = str(skill_dir)
            
            self.skills[skill_info['name']] = skill_info
            self.logger.info(f"注册技能：{skill_info['name']} (source={source})")
    
    def register_workspace_skills(self, skills_dir: Path):
        """注册工作目录级别的技能（优先级高于全局技能）
        
        Args:
            skills_dir: 技能目录
        """
        if not skills_dir.exists():
            return
        
        for skill_dir in skills_dir.iterdir():
            if skill_dir.is_dir() and (skill_dir / "main.py").exists():
                self.register_skill_from_dir(skill_dir, source="workspace")
    
    def unregister_workspace_skills(self):
        """注销工作目录级别的技能"""
        workspace_skills = [
            name for name, info in self.skills.items()
            if info.get("source") == "workspace"
        ]
        
        for skill_name in workspace_skills:
            del self.skills[skill_name]
            self.logger.debug(f"已注销工作目录技能：{skill_name}")
```

---

## 五、API 端点设计

**新增文件**: `lingxi/web/routes/workspace.py`

```python
"""工作目录 API 路由"""

from fastapi import APIRouter, HTTPException
from pydantic import BaseModel
from typing import Optional, Dict, Any
import logging

logger = logging.getLogger(__name__)

router = APIRouter(prefix="/api/workspace", tags=["workspace"])


class WorkspaceSwitchRequest(BaseModel):
    """切换工作目录请求"""
    workspace_path: str
    force: bool = False


class WorkspaceSwitchResponse(BaseModel):
    """切换工作目录响应"""
    success: bool
    data: Optional[Dict[str, Any]] = None
    error: Optional[str] = None


class WorkspaceInfoResponse(BaseModel):
    """工作目录信息响应"""
    workspace: Optional[str]
    lingxi_dir: Optional[str]
    is_initialized: bool


@router.post("/switch", response_model=WorkspaceSwitchResponse)
async def switch_workspace(request: WorkspaceSwitchRequest):
    """切换工作目录
    
    请求示例:
    ```json
    {
        "workspace_path": "D:/projects/my-project",
        "force": false
    }
    ```
    
    返回示例:
    ```json
    {
        "success": true,
        "data": {
            "previous_workspace": "D:/projects/old-project",
            "current_workspace": "D:/projects/my-project",
            "lingxi_dir": "D:/projects/my-project/.lingxi",
            "switched_at": "2026-03-07T10:00:00"
        }
    }
    ```
    """
    try:
        from lingxi.management.workspace import get_workspace_manager
        
        workspace_manager = get_workspace_manager()
        result = await workspace_manager.switch_workspace(
            request.workspace_path,
            force=request.force
        )
        
        return WorkspaceSwitchResponse(success=True, data=result)
    except Exception as e:
        logger.error(f"切换工作目录失败：{e}")
        raise HTTPException(status_code=500, detail=f"切换失败：{str(e)}")


@router.get("/current", response_model=WorkspaceInfoResponse)
async def get_current_workspace():
    """获取当前工作目录
    
    返回示例:
    ```json
    {
        "workspace": "D:/projects/my-project",
        "lingxi_dir": "D:/projects/my-project/.lingxi",
        "is_initialized": true
    }
    ```
    """
    from lingxi.management.workspace import get_workspace_manager
    
    workspace_manager = get_workspace_manager()
    workspace = workspace_manager.get_current_workspace()
    
    if workspace is None:
        return WorkspaceInfoResponse(
            workspace=None,
            lingxi_dir=None,
            is_initialized=False
        )
    
    return WorkspaceInfoResponse(
        workspace=str(workspace),
        lingxi_dir=str(workspace_manager.get_lingxi_directory()),
        is_initialized=True
    )


@router.post("/initialize")
async def initialize_workspace(workspace_path: Optional[str] = None):
    """初始化工作目录
    
    请求示例:
    ```json
    {
        "workspace_path": "D:/projects/new-project"
    }
    ```
    
    返回示例:
    ```json
    {
        "success": true,
        "data": {
            "workspace": "D:/projects/new-project",
            "lingxi_dir": "D:/projects/new-project/.lingxi"
        }
    }
    ```
    """
    try:
        from lingxi.management.workspace import get_workspace_manager
        
        workspace_manager = get_workspace_manager()
        lingxi_dir = workspace_manager.initialize(workspace_path)
        
        return {
            "success": True,
            "data": {
                "workspace": str(workspace_manager.get_current_workspace()),
                "lingxi_dir": str(lingxi_dir)
            }
        }
    except Exception as e:
        logger.error(f"初始化工作目录失败：{e}")
        raise HTTPException(status_code=500, detail=str(e))


@router.get("/validate")
async def validate_workspace(workspace_path: str):
    """验证工作目录是否有效
    
    查询参数:
    - workspace_path: 工作目录路径
    
    返回示例:
    ```json
    {
        "valid": true,
        "exists": true,
        "has_lingxi_dir": true,
        "message": "工作目录有效"
    }
    ```
    """
    from pathlib import Path
    from lingxi.management.workspace import get_workspace_manager
    
    workspace_manager = get_workspace_manager()
    path = Path(workspace_path).resolve()
    
    exists = path.exists()
    has_lingxi_dir = (path / ".lingxi").exists() if exists else False
    valid = workspace_manager.validate_workspace(path)
    
    return {
        "valid": valid,
        "exists": exists,
        "has_lingxi_dir": has_lingxi_dir,
        "message": "工作目录有效" if valid else "工作目录无效"
    }
```

---

## 六、执行流程

### 6.1 启动初始化流程

```
1. 应用启动
   ↓
2. 读取全局配置 (config.yaml)
   ↓
3. 检查 last_workspace 配置
   ↓
4. 如果存在，加载该工作目录
   ↓
5. 如果不存在，使用当前目录
   ↓
6. 初始化 .lingxi 目录结构
   ↓
7. 加载工作目录配置
   ↓
8. 更新 SecuritySandbox.workspace_root
   ↓
9. 注册工作目录技能
   ↓
10. 初始化数据库连接
   ↓
11. 启动完成
```

### 6.2 切换工作目录流程

```
1. 接收 API 请求 POST /api/workspace/switch
   ↓
2. 检查是否有执行中任务
   ↓
3. 如果有任务，等待任务完成（最多 5 分钟）
   ↓
4. 验证目标路径存在
   ↓
5. 保存当前会话状态到数据库
   ↓
6. 关闭当前数据库连接
   ↓
7. 注销工作目录技能
   ↓
8. 初始化目标 .lingxi 目录（如不存在）
   ↓
9. 加载目标工作目录配置
   ↓
10. 更新 SecuritySandbox.workspace_root
   ↓
11. 注册新工作目录技能
   ↓
12. 初始化新数据库连接
   ↓
13. 持久化工作目录路径到 config.yaml
   ↓
14. 发布 workspace_switched 事件
   ↓
15. 返回切换结果
```

---

## 七、配置管理

### 7.1 全局配置 (`config.yaml`)

```yaml
# 全局配置
workspace:
  last_workspace: "D:/projects/my-project"  # 上次使用的工作目录

# 默认安全配置（可被工作目录覆盖）
security:
  workspace_root: "./workspace"
  safety_mode: true
  max_file_size: 10485760
  allowed_commands:
    - "ls"
    - "pwd"
    - "git"
    - "cat"
    - "grep"

# 数据库配置
database:
  path: "./data/lingxi.db"
```

### 7.2 工作目录配置 (`.lingxi/conf/config.yml`)

```yaml
# 工作目录专属配置（优先级高）
workspace:
  name: "我的项目"
  description: "项目专属工作空间"

# 覆盖全局安全配置
security:
  safety_mode: false  # 开发模式关闭
  max_file_size: 104857600  # 100MB

# 技能配置
skills:
  enabled:
    - "custom_data_processor"
    - "project_analyzer"
```

### 7.3 配置合并逻辑

```python
def merge_config(global_config: Dict, workspace_config: Dict) -> Dict:
    """合并配置（工作目录配置覆盖全局配置）"""
    import copy
    
    merged = copy.deepcopy(global_config)
    
    for key, value in workspace_config.items():
        if isinstance(value, dict) and key in merged:
            merged[key] = merge_config(merged[key], value)
        else:
            merged[key] = value
    
    return merged
```

---

## 八、异常处理

### 8.1 异常类型定义

```python
class WorkspaceError(Exception):
    """工作目录异常基类"""
    pass

class WorkspaceNotFoundError(WorkspaceError):
    """工作目录不存在"""
    error_code = "WORKSPACE_NOT_FOUND"

class WorkspaceInitError(WorkspaceError):
    """工作目录初始化失败"""
    error_code = "WORKSPACE_INIT_FAILED"

class WorkspaceSwitchError(WorkspaceError):
    """工作目录切换失败"""
    error_code = "WORKSPACE_SWITCH_FAILED"

class LingxiDirectoryNotFoundError(WorkspaceError):
    """.lingxi 目录不存在"""
    error_code = "LINGXI_DIR_NOT_FOUND"
```

### 8.2 错误码定义

| 错误码 | 说明 | 处理建议 |
|-------|------|---------|
| `WORKSPACE_NOT_FOUND` | 工作目录不存在 | 检查路径是否正确 |
| `LINGXI_DIR_NOT_FOUND` | `.lingxi` 目录不存在 | 自动创建或手动初始化 |
| `SWITCH_FAILED_RUNNING_TASKS` | 切换失败（有任务在执行） | 等待任务完成或使用 force=true |
| `CONFIG_LOAD_FAILED` | 配置文件加载失败 | 检查 YAML 格式 |
| `DATABASE_INIT_FAILED` | 数据库初始化失败 | 检查文件权限 |
| `SKILL_REGISTER_FAILED` | 技能注册失败 | 检查技能目录结构 |

---

## 九、测试用例

### 9.1 单元测试

```python
"""工作目录功能测试用例"""

import pytest
import tempfile
import asyncio
from pathlib import Path
from lingxi.management.workspace import WorkspaceManager, WorkspaceError


class TestWorkspaceManager:
    """工作目录管理器测试"""
    
    @pytest.fixture
    def workspace_manager(self):
        """创建工作目录管理器"""
        config = {
            "security": {
                "workspace_root": "./workspace",
                "safety_mode": True
            }
        }
        return WorkspaceManager(config)
    
    def test_initialize_workspace(self, workspace_manager):
        """测试初始化工作目录"""
        with tempfile.TemporaryDirectory() as tmpdir:
            lingxi_dir = workspace_manager.initialize(tmpdir)
            
            assert (lingxi_dir / "conf").exists()
            assert (lingxi_dir / "data").exists()
            assert (lingxi_dir / "skills").exists()
            assert (lingxi_dir / "conf" / "config.yml").exists()
    
    def test_initialize_existing_workspace(self, workspace_manager):
        """测试初始化已存在的工作目录"""
        with tempfile.TemporaryDirectory() as tmpdir:
            # 第一次初始化
            lingxi_dir1 = workspace_manager.initialize(tmpdir)
            
            # 第二次初始化（已存在）
            lingxi_dir2 = workspace_manager.initialize(tmpdir)
            
            assert lingxi_dir1 == lingxi_dir2
    
    @pytest.mark.asyncio
    async def test_switch_workspace(self, workspace_manager):
        """测试切换工作目录"""
        with tempfile.TemporaryDirectory() as tmpdir1:
            with tempfile.TemporaryDirectory() as tmpdir2:
                # 初始化第一个工作目录
                workspace_manager.initialize(tmpdir1)
                
                # 切换到第二个工作目录
                result = await workspace_manager.switch_workspace(tmpdir2)
                
                assert result["success"] is True
                assert workspace_manager.get_current_workspace() == Path(tmpdir2)
    
    @pytest.mark.asyncio
    async def test_switch_workspace_not_exists(self, workspace_manager):
        """测试切换不存在的目录"""
        with tempfile.TemporaryDirectory() as tmpdir:
            workspace_manager.initialize(tmpdir)
            
            with pytest.raises(WorkspaceNotFoundError):
                await workspace_manager.switch_workspace("/non/existent/path")
    
    def test_validate_workspace_valid(self, workspace_manager):
        """测试验证有效的工作目录"""
        with tempfile.TemporaryDirectory() as tmpdir:
            workspace_manager.initialize(tmpdir)
            
            assert workspace_manager.validate_workspace(Path(tmpdir)) is True
    
    def test_persist_workspace_path(self, workspace_manager):
        """测试持久化工作目录"""
        with tempfile.TemporaryDirectory() as tmpdir:
            workspace_manager.initialize(tmpdir)
            
            # 持久化
            workspace_manager._persist_workspace_path(Path(tmpdir))
            
            # 验证配置文件已更新
            config_file = Path(__file__).parent.parent / "config.yaml"
            with open(config_file, 'r') as f:
                import yaml
                config = yaml.safe_load(f)
            
            assert config['workspace']['last_workspace'] == tmpdir
```

### 9.2 集成测试

```python
"""工作目录集成测试"""

import pytest
from pathlib import Path


class TestWorkspaceIntegration:
    """工作目录集成测试"""
    
    def test_security_sandbox_workspace_limit(self):
        """测试 SecuritySandbox 限制在工作目录内"""
        # TODO: 实现集成测试
        
    def test_workspace_skills_priority(self):
        """测试工作目录技能优先级"""
        # TODO: 实现集成测试
    
    def test_config_merge(self):
        """测试配置合并逻辑"""
        from lingxi.management.workspace import WorkspaceManager
        
        config = {
            "security": {
                "safety_mode": True,
                "max_file_size": 10485760
            }
        }
        
        workspace_config = {
            "security": {
                "safety_mode": False
            }
        }
        
        merged = WorkspaceManager(config)._deep_merge(config, workspace_config)
        
        assert merged['security']['safety_mode'] is False  # 被覆盖
        assert merged['security']['max_file_size'] == 10485760  # 保留
```

---

## 十、部署与运维

### 10.1 启动脚本

```bash
# 使用当前目录作为工作目录
python -m lingxi

# 指定工作目录
python -m lingxi --workspace /path/to/project

# 桌面版（通过配置文件指定）
lingxi-desktop.exe --config config.yaml
```

### 10.2 配置文件示例

```yaml
# config.yaml
workspace:
  last_workspace: "D:/projects/my-project"

# 启动时自动加载 last_workspace
```

### 10.3 日志输出

```
2026-03-07 10:00:00 - lingxi.management.workspace - INFO - 工作目录初始化：D:/projects/my-project
2026-03-07 10:00:01 - lingxi.management.workspace - INFO - .lingxi 目录创建完成
2026-03-07 10:00:01 - lingxi.management.workspace - INFO - 工作目录配置已加载
2026-03-07 10:00:02 - lingxi.core.security - INFO - SecuritySandbox 工作目录：D:/projects/my-project
2026-03-07 10:05:00 - lingxi.management.workspace - INFO - 工作目录切换：D:/projects/my-project -> D:/projects/another-project
```

### 10.4 监控指标

| 指标 | 说明 | 告警阈值 |
|------|------|---------|
| 工作目录切换次数 | 单位时间内切换次数 | > 10 次/分钟 |
| 切换成功率 | 成功切换次数 / 总切换次数 | < 95% |
| 平均切换耗时 | 从请求到完成的平均时间 | > 30 秒 |
| 工作目录技能数量 | 当前加载的工作目录技能数 | > 50 个 |

---

## 十一、开发任务清单

### Phase 1: 核心实现

- [ ] 创建 `lingxi/management/workspace.py`
- [ ] 实现 `WorkspaceManager` 类
- [ ] 实现 `WorkspaceInitializer` 类
- [ ] 扩展 `SecuritySandbox.update_workspace()`
- [ ] 扩展 `SkillRegistry.register_workspace_skills()`

### Phase 2: API 集成

- [ ] 创建 `lingxi/web/routes/workspace.py`
- [ ] 实现 `/api/workspace/switch` 端点
- [ ] 实现 `/api/workspace/current` 端点
- [ ] 实现 `/api/workspace/initialize` 端点
- [ ] 实现 `/api/workspace/validate` 端点

### Phase 3: 配置持久化

- [ ] 实现 `_persist_workspace_path()` 方法
- [ ] 修改启动逻辑读取 `last_workspace`
- [ ] 实现配置合并逻辑

### Phase 4: 测试验证

- [ ] 编写单元测试
- [ ] 编写集成测试
- [ ] 手动测试切换流程
- [ ] 验证安全沙箱限制

### Phase 5: 文档与部署

- [ ] 更新用户文档
- [ ] 更新 API 文档
- [ ] 编写部署脚本
- [ ] 发布新版本

---

## 十二、自检验表

- [x] 模块是否单一职责？ → `WorkspaceManager` 只负责工作目录管理
- [x] 新增是否扩展而非修改？ → 通过事件驱动扩展，不修改现有组件
- [x] 接口是否有契约测试？ → 提供 API 端点测试
- [x] 是否识别变化点并预留扩展？ → 支持多工作空间、配置覆盖
- [x] 核心链路是否有熔断/限流/降级？ → 切换超时保护
- [x] 日志监控告警是否全覆盖？ → 所有关键操作都有日志
- [x] 设计简洁易维护？ → 遵循现有架构模式
- [x] 安全性是否保障？ → SecuritySandbox 限制在工作目录内

---

## 附录

### A. 术语表

| 术语 | 说明 |
|------|------|
| Workspace | 工作目录，Agent 的工作空间根目录 |
| .lingxi | 灵犀专属目录，存放配置、数据、技能 |
| 工作目录技能 | 存放在 `.lingxi/skills/` 的自定义技能 |
| 全局技能 | 存放在系统全局的技能 |

### B. 参考资料

- [灵犀个人智能助手详细设计.md](./灵犀个人智能助手详细设计.md) - 第七章 安全设计
- [API 接口设计.md](./API 接口设计.md) - RESTful API 规范
- [WebSocket_API.md](./WebSocket_API.md) - WebSocket 事件定义

### C. 版本历史

| 版本 | 日期 | 作者 | 变更说明 |
|------|------|------|---------|
| V1.0 | 2026-03-07 | AI Assistant | 初始版本 |

---

**设计完成！** 🎉
